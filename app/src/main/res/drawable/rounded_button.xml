<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Button background color -->
    <solid android:color="#fff"/>   <!-- Brown color (same as in your design) -->

    <!-- Rounded corners -->
    <corners android:radius="25dp" />

    <!-- Optional padding -->
    <padding
        android:left="16dp"
        android:top="12dp"
        android:right="16dp"
        android:bottom="12dp" />

</shape>
